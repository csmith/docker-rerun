---
machine:
  python:
    version: 3.4.3
  services:
    - docker

dependencies:
  pre:
    - pip install nose pylint

test:
  override:
    - pylint -d invalid-name docker-rerun
    - nosetests test.py

